import UIKit

/// Adopts UIKit’s scene-based lifecycle (required for future iOS; keeps Main.storyboard + `CAPBridgeViewController`).
class SceneDelegate: UIResponder, UIWindowSceneDelegate {

    var window: UIWindow?
}

project_id = "uncnkmgaoawksbfncnkm"

# Database webhooks call this function with `x-webhook-secret` (no user JWT).
[functions.broadcast-push]
verify_jwt = false

# Public photo submissions — anyone can upload from the website.
[functions.submit-photo]
verify_jwt = false